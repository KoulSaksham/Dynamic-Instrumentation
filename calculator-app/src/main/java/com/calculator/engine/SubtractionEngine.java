package com.calculator.engine;

/** Layer 3 leaf: subtraction. NO observability code. */
public class SubtractionEngine {
    public double subtract(double a, double b) {
        double difference = a - b;
        return difference;
    }
}
